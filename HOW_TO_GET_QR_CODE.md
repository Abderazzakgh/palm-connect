# كيفية الحصول على كود QR للماسح

## 📋 نظرة عامة

هناك طريقتان للحصول على الباركود (QR Code) في النظام:

### 1️⃣ من صفحة المسح (`/scanner`)

عندما تمسح كف يدك لأول مرة في صفحة `/scanner`:

1. **اذهب إلى صفحة المسح**: `/scanner`
2. **فعل الجهاز**: اضغط على زر "فعل الجهاز"
3. **ابدأ المسح**: اضغط على "مرر كف يدك (تسجيل أولي)"
4. **احصل على الباركود**: بعد اكتمال المسح، سيظهر لك:
   - **QR Code مرئي** على الشاشة
   - **رمز نصي** يمكن نسخه

#### مثال على الباركود:
```
BIOMETRIC_1234567890_abc123
```

أو في صيغة JSON:
```json
{
  "type": "SAVANNA_BIOMETRIC",
  "uid": "uid-abc123",
  "hash": "PALM_1234567890_xyz"
}
```

### 2️⃣ من قاعدة البيانات

يمكنك جلب الباركود مباشرة من قاعدة البيانات:

```typescript
// جلب جميع الباركودات المعلقة
const { data: palmPrints } = await supabase
  .from('palm_prints')
  .select('qr_code, palm_hash, created_at, expires_at')
  .eq('status', 'pending')
  .order('created_at', { ascending: false });

// جلب باركود محدد
const { data: palmPrint } = await supabase
  .from('palm_prints')
  .select('qr_code')
  .eq('qr_code', 'BIOMETRIC_XXXXXXXXXXXX')
  .eq('status', 'pending')
  .maybeSingle();
```

## 🔍 كيفية استخدام الماسح

### الخطوات:

1. **افتح صفحة ربط البصمة** (في Dashboard بعد إكمال الملف الشخصي)

2. **اضغط على زر المسح** (أيقونة الكاميرا) بجانب حقل الباركود

3. **امنح صلاحيات الكاميرا** عند الطلب

4. **ضع الباركود داخل الإطار**:
   - يمكنك مسح الباركود من الشاشة (من صفحة `/scanner`)
   - أو من صورة مطبوعة
   - أو من جهاز آخر

5. **سيتم قراءة الباركود تلقائياً**:
   - سيتم ملء الحقل تلقائياً
   - سيتم إغلاق الماسح
   - يمكنك الضغط على "ربط البصمة"

## 📱 أمثلة على الباركود

### النوع الأول: نص بسيط
```
BIOMETRIC_1701234567890_a1b2c3d4
```

### النوع الثاني: JSON
```json
{
  "type": "SAVANNA_BIOMETRIC",
  "uid": "uid-abc123",
  "hash": "PALM_1701234567890_xyz789"
}
```

## 🛠️ للاختبار (Development)

يمكنك إنشاء باركود تجريبي:

```typescript
// في console المتصفح
const testQR = `BIOMETRIC_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
console.log("Test QR Code:", testQR);
```

أو استخدام JSON:

```typescript
const testQRJSON = JSON.stringify({
  type: "SAVANNA_BIOMETRIC",
  uid: "uid-test123",
  hash: `PALM_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`
});
console.log("Test QR Code (JSON):", testQRJSON);
```

## 🔐 ملاحظات أمنية

- الباركود صالح لمدة **24 ساعة** فقط
- الباركود يجب أن يكون في حالة `pending` (غير مكتمل)
- بعد الربط، يتغير الحالة إلى `completed`

## 📝 Debugging

لرؤية الباركود الممسوح في Console:

1. افتح Developer Tools (F12)
2. اذهب إلى Console
3. عند مسح الباركود، ستظهر رسالة:
   ```
   ✅ تم مسح الباركود بنجاح: [الباركود هنا]
   📱 الباركود الممسوح: [الباركود هنا]
   ```

## 🎯 سير العمل الكامل

```
1. المستخدم يزور /scanner
   ↓
2. يمسح كف يده
   ↓
3. يحصل على QR Code (على الشاشة أو نص)
   ↓
4. يذهب إلى /auth (إنشاء حساب)
   ↓
5. يكمل الملف الشخصي في /dashboard
   ↓
6. يربط الباركود (يدوياً أو بالمسح)
   ↓
7. يتم الربط بنجاح ✅
```

---

**نصيحة**: احفظ الباركود فور الحصول عليه لأنه صالح لمدة 24 ساعة فقط!

