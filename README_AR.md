# سافانا - نظام المصادقة البيومترية

## نظرة عامة

**سافانا (Savanna)** هو نظام متكامل للمصادقة البيومترية باستخدام بصمة الكف، مع ربط سلس بحسابات المستخدمين البنكية. يوفر النظام حلولاً آمنة للدفع، التحكم بالدخول، وإدارة المعاملات.

## المميزات الرئيسية

### 🔐 المصادقة البيومترية
- مسح بصمة الكف باستخدام تقنيات متقدمة
- تشفير آمن للبيانات البيومترية (ECDH + HKDF + AES-GCM)
- ربط فريد لكل مستخدم

### 💳 نقطة البيع (POS)
- دفع سريع وآمن بالبصمة الحيوية
- ربط مباشر بالحسابات البنكية
- سجل كامل للمعاملات

### 🚪 التحكم بالدخول
- نظام دخول وخروج بالبصمة
- تتبع كامل للأنشطة
- أمان عالي المستوى

### 📊 لوحة التحكم
- عرض شامل للملف الشخصي
- سجل المعاملات والأنشطة
- إدارة سهلة للحساب

## التقنيات المستخدمة

### Frontend
- **React 18** - مكتبة واجهة المستخدم
- **TypeScript** - للكتابة الآمنة
- **Vite** - أداة البناء السريعة
- **Tailwind CSS** - للتصميم
- **shadcn/ui** - مكونات واجهة المستخدم
- **React Router** - للتنقل
- **Supabase** - قاعدة البيانات والمصادقة

### Backend Services
- **Express.js** - خادم التطبيق
- **Node.js** - بيئة التشغيل
- **SQLite** - قاعدة بيانات محلية للصلاحيات
- **Algorithm Service** - خدمة تحليل البصمات

### الأمان
- **ECDH (P-256)** - تبادل المفاتيح
- **HKDF-SHA256** - اشتقاق المفاتيح
- **AES-GCM 256** - التشفير المتماثل
- **Row Level Security (RLS)** - أمان على مستوى الصفوف

## متطلبات التشغيل

- Node.js 18+ 
- npm أو yarn
- حساب Supabase (للحصول على URL و API Key)

## التثبيت والإعداد

### 1. تثبيت التبعيات

```bash
# تثبيت تبعيات الواجهة الأمامية
npm install

# تثبيت تبعيات الخادم
cd server
npm install
cd ..
```

### 2. إعداد Supabase

1. أنشئ مشروع جديد على [Supabase](https://supabase.com)
2. احصل على `Project URL` و `anon key`
3. قم بتشغيل ملفات الهجرة (migrations) من مجلد `supabase/migrations`

### 3. إعداد المتغيرات البيئية

أنشئ ملف `.env` في المجلد الرئيسي:

```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
```

### 4. تشغيل ملفات الهجرة

في Supabase Dashboard:
1. اذهب إلى SQL Editor
2. قم بتشغيل الملفات بالترتيب:
   - `20251106011503_9c95fabf-9c8c-453a-a924-34bf95c80cbb.sql`
   - `20251106011514_b0171131-16e5-41ee-bdd3-7455087c281c.sql`
   - `20251106012753_5e38d36b-f035-4877-9315-52f9037c3095.sql`
   - `20251106020000_add_matched_user_id.sql`

## التشغيل

### تطوير الواجهة الأمامية

```bash
npm run dev
```

التطبيق سيعمل على `http://localhost:5173`

### تشغيل الخوادم الخلفية

```bash
# تشغيل جميع الخدمات
cd server
npm run start:all

# أو تشغيل كل خدمة على حدة
npm run start:app    # Application Service على المنفذ 4000
npm run start:algo   # Algorithm Service على المنفذ 4100
```

## هيكل المشروع

```
palm-connect/
├── src/                    # كود الواجهة الأمامية
│   ├── components/         # المكونات القابلة لإعادة الاستخدام
│   ├── pages/              # صفحات التطبيق
│   ├── integrations/       # تكامل Supabase
│   └── lib/                # أدوات مساعدة
├── server/                 # الخوادم الخلفية
│   ├── app-service/        # خدمة التطبيق الرئيسية
│   ├── algo-service/       # خدمة تحليل البصمات
│   └── test-client/         # اختبارات التكامل
├── supabase/
│   └── migrations/         # ملفات هجرة قاعدة البيانات
└── public/                 # الملفات الثابتة
```

## سير العمل

### 1. التسجيل الأولي
1. المستخدم يزور صفحة `/scanner`
2. يمسح كف يده على الجهاز
3. يحصل على باركود فريد (QR Code)
4. الباركود صالح لمدة 24 ساعة

### 2. إنشاء الحساب
1. المستخدم يزور `/auth` لإنشاء حساب
2. يسجل بياناته الشخصية في `/dashboard`
3. يربط الباركود الذي حصل عليه من المسح

### 3. الاستخدام
- **الدفع**: استخدام `/pos` للمعاملات المالية
- **التحكم بالدخول**: استخدام `/access` للدخول والخروج
- **السجلات**: عرض جميع المعاملات في `/transactions`

## الأمان

### التشفير
- جميع البيانات البيومترية مشفرة قبل الإرسال
- استخدام ECDH لتبادل المفاتيح بشكل آمن
- HKDF لاشتقاق مفاتيح AES من المفتاح المشترك
- AES-GCM للتشفير المتماثل

### قاعدة البيانات
- Row Level Security (RLS) مفعّل على جميع الجداول
- المستخدمون يمكنهم فقط الوصول لبياناتهم
- الصلاحيات محددة بدقة

### الخدمات
- كل خدمة تعمل بشكل مستقل
- اتصال آمن بين الخدمات
- تسجيل كامل للأحداث (audit logging)

## الاختبار

```bash
# اختبارات التكامل
cd server
npm run test:integration

# اختبار X-Forwarded-For
npm run test:xfwd
```

## النشر

### بناء المشروع

```bash
npm run build
```

الملفات المبنية ستكون في مجلد `dist/`

### نشر Supabase

1. اربط مشروع Supabase مع Git
2. الملفات في `supabase/migrations` ستعمل تلقائياً

### نشر الخوادم

يمكن نشر الخوادم على:
- Heroku
- Railway
- AWS
- أي منصة Node.js

## المساهمة

نرحب بالمساهمات! يرجى:
1. عمل Fork للمشروع
2. إنشاء branch للميزة الجديدة
3. عمل Commit للتغييرات
4. عمل Push للـ branch
5. فتح Pull Request

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## الدعم

للأسئلة والدعم:
- افتح Issue على GitHub
- راجع الوثائق في `docs/`

## التطوير المستقبلي

- [ ] دعم أجهزة مسح بصمة الكف الفعلية
- [ ] تطبيق جوال (React Native)
- [ ] لوحة تحكم إدارية
- [ ] تقارير وتحليلات متقدمة
- [ ] دعم عملات متعددة
- [ ] تكامل مع أنظمة الدفع الأخرى

---

**صُنع بـ ❤️ باستخدام React و Supabase**

