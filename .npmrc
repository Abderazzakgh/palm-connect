# Disable optional dependencies to prevent Rollup native module issues
optional=false
legacy-peer-deps=true

# Speed up installation
prefer-offline=true
audit=false
fund=false

# Ensure compatibility
engine-strict=false
