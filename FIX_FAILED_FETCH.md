# 🔧 حل مشكلة "Failed to fetch" - دليل سريع

## ✅ تم إصلاح الكود!

تم تحديث `CompleteProfile.tsx` لتحسين معالجة الأخطاء وإظهار رسائل واضحة.

---

## 🎯 الحل السريع (3 خطوات)

### الخطوة 1: إنشاء ملف `.env` ⚡

في المجلد الرئيسي للمشروع، أنشئ ملف `.env`:

```env
VITE_SUPABASE_URL=https://qxtdcqwhqfuhlhwoffem.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_key_here
```

**للحصول على anon key:**
1. Supabase Dashboard → Settings → API
2. انسخ `anon` `public` key
3. الصقه في `.env`

---

### الخطوة 2: نشر Edge Functions ⚡

**في Supabase Dashboard:**

1. **Edge Functions** → **Create a new function**

**لكل function (3 functions):**
- الاسم: `handshake`
- انسخ الكود من: `supabase/functions/handshake/index.ts`
- **Deploy**

كرر للـ functions الأخرى:
- `vein-upload`
- `vein-secure-upload`

---

### الخطوة 3: إعادة تشغيل التطبيق ⚡

```bash
# أوقف التطبيق (Ctrl+C)
npm run dev
```

**مهم:** يجب إعادة تشغيل التطبيق بعد إضافة/تعديل `.env`!

---

## 🔍 التحقق من الحل

بعد تطبيق الخطوات:

1. **افتح التطبيق:** http://localhost:5173
2. **Complete Profile** → التقط صورة
3. **إرسال آمن (ECDH)**

**يجب أن ترى:**
- ✅ رسالة نجاح: "تم الإرسال الآمن بنجاح"
- ❌ **لا** ترى "Failed to fetch"

---

## 📋 قائمة التحقق

- [ ] ملف `.env` موجود في المجلد الرئيسي
- [ ] `VITE_SUPABASE_URL` صحيح (يبدأ بـ `https://`)
- [ ] `VITE_SUPABASE_PUBLISHABLE_KEY` موجود وصحيح
- [ ] Edge Functions منشورة (3 functions)
- [ ] تم إعادة تشغيل التطبيق بعد إضافة `.env`

---

## 🆘 إذا استمرت المشكلة

### 1. تحقق من Console (F12)
- ابحث عن أخطاء JavaScript
- ابحث عن رسائل الخطأ الجديدة (الآن أكثر وضوحاً)

### 2. تحقق من Network Tab (F12)
- ابحث عن الطلب الفاشل
- تحقق من Status Code:
  - **404** = Function غير موجود (لم يتم نشره)
  - **401** = anon key غير صحيح
  - **500** = خطأ في Edge Function (راجع Logs)

### 3. تحقق من Edge Functions Logs
- Supabase Dashboard → Edge Functions → اختر function → Logs
- ابحث عن أخطاء

---

## 💡 نصائح

1. **الأكثر شيوعاً:** ملف `.env` غير موجود أو لم يتم إعادة تشغيل التطبيق
2. **ثانياً:** Edge Functions غير منشورة
3. **ثالثاً:** anon key غير صحيح

---

## 📚 ملفات مساعدة

- **`CHECK_SETUP.md`** - فحص سريع للإعداد
- **`TROUBLESHOOTING.md`** - حلول تفصيلية
- **`INSTRUCTIONS_AR.md`** - تعليمات خطوة بخطوة

---

**✅ بعد تطبيق الخطوات الثلاث، يجب أن يعمل كل شيء!**

